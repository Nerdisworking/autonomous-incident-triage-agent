FROM eclipse-temurin:21-jdk AS build
WORKDIR /workspace
COPY . .
RUN ./gradlew :services:agent-orchestrator:bootJar || true

FROM eclipse-temurin:21-jre
WORKDIR /app
COPY services/agent-orchestrator/build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
